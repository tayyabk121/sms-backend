package com.example.sms.util;

/** Enum representing the status of a payment in the application. This enum
 * is used to track the different stages of a payment process, such as when a
 * payment is pending, processed, or paid.
 */
public enum PaymentStatus {
    PENDING,
    PROCESSED,
    PAID
}
