package com.example.sms.util;

/** Enum representing the different payment methods available in the
 * application. This enum is used to specify the method of payment for
 * transactions, such as online payments, cash payments, cheque payments, and
 * demand drafts (DD). */
public enum PaymentMethod {
    ONLINE,
    CASH,
    CHEQUE,
    DD
}
