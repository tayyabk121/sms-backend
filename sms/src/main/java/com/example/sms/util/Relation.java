package com.example.sms.util;

/** Enum representing the relationship of a contact person to the user. This
 * enum is used to categorize contacts as either a father, mother, or
 * guardian. It can be utilized in various parts of the application where
 * contact information is required, such as in user profiles, emergency
 * contacts, or any other relevant context. */
public enum Relation {
    FATHER,
    MOTHER,
    GUARDIAN
}
