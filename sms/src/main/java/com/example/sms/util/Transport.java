package com.example.sms.util;

/** Enum representing different modes of transportation. This enum can be used to
 * specify the mode of transport for a particular context, such as a school
 * trip or daily commute. The available options include SCHOOL_BUS, SELF, and
 * WALKING. Each constant represents a specific mode of transportation that can
 * be selected or assigned in the application. */
public enum Transport {
    SCHOOL_BUS,
    SELF,
    WALKING
}
