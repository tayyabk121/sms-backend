package com.example.sms.util;

/** Enum representing the status of payroll runs in the application.
 * This enum is used to track the different stages of payroll processing,
 * including draft, processing, and completed states. */
public enum PayrollRunsStatus {
    DRAFT,
    PROCESSING,
    COMPLETED
}
