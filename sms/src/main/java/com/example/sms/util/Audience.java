package com.example.sms.util;

/** Enum representing the different audiences for notifications in the application.
 * This enum is used to specify the target audience when sending notifications,
 * allowing for targeted communication to specific groups such as parents, staff,
 * or students.
 */
public enum Audience {
    ALL,
    PARENTS,
    STAFF,
    STUDENTS
}
