package com.example.sms.util;

/** Enum representing the status of a branch in the application. This enum
 * is used to indicate whether a branch is active or inactive. */
public enum BranchStatus {
    ACTIVE,
    INACTIVE
}
