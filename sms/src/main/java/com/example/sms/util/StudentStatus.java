package com.example.sms.util;

/** Enum representing the status of a student in the Student Management
 * System. This enum is used to track the current state of a student, such as
 * whether they are active, inactive, transferred, or graduated. */
public enum StudentStatus {
    ACTIVE,
    INACTIVE,
    TRANSFERRED,
    GRADUATED
}
