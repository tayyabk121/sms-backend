package com.example.sms.util;

/** Enum representing the days of the week. This enum is used to standardize the
 * representation of days in the application, allowing for consistent usage
 * across different components and modules.
 */
public enum DayOfWeek {
    MONDAY,
    TUESDAY,
    WEDNESDAY,
    THURSDAY,
    FRIDAY,
    SATURDAY
}
