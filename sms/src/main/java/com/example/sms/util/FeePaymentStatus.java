package com.example.sms.util;

/** Enum representing the status of fee payments in the application.
 * This enum is used to track the various stages of fee payment processing,
 * including pending, paid, failed, and refunded statuses. */
public enum FeePaymentStatus {
    PENDING,
    PAID,
    FAILED,
    REFUNDED
}
