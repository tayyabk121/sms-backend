package com.example.sms.util;

/** * Enum representing the possible grades for a student. This enum can be used to
 * standardize the grading system across the application and ensure consistency
 * when assigning grades to students.
 */
public enum GradeEnum {
    A_PLUS,
    A,
    B_PLUS,
    B,
    C,
    D,
    F
}
